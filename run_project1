node {
	stage 'starting'
	def project_folder = input message: 'User input required', ok: 'Release!',parameters: [choice(name: 'RELEASE_SCOPE', choices: description: 'What is the release scope?')]
	echo "${project_folder}"
}